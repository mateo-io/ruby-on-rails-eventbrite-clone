<h1>Create Event</h1>

<%= form_for(@event) do |f| %>

<%= f.label :description %>
<%= f.text_field :description %>

<%= f.label :date %>
<%= f.date_select(:date, use_month_numbers: false, start_year: 2015) %>

<%= f.submit "Create" %>

<% end %>